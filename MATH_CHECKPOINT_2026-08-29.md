# Mathematics Lane Checkpoint — 2026-08-29

**Status:** Provisional mathematical checkpoint generated by an adversarial theory lane and frozen with its raw output in `research_logs/MATH_LANE_CHECKPOINT_2026-08-29.txt`. Formal proofs and primary-literature citations still require independent checking before manuscript submission.

## Executive result

The mathematical novelty became substantially narrower. Reeb spaces, predictive sufficiency, causal states, predictive-state representations (PSRs), input-output computational mechanics, augmented-state observability/identifiability, and active experimental design are strong precedents. We should not claim ownership of those ideas.

What remains scientifically interesting is an application/synthesis question:

> Does there exist a **biologically measurable current developmental state** whose fibers approximate the minimal interventionally predictive equivalence classes for a specified family of future developmental interventions?

The connected finite-resolution inverse fibers of a joint state-law model may still be a useful object for diagnosing what the current experiment cannot distinguish and for choosing follow-up experiments, but the surrounding mathematics must be described in relation to established theory.

## 1. Exact refinement under a monotone experiment metric

For an experiment panel `Q`, target world `w*`, tolerance `delta`, and a product/sup metric over experiment outputs, define

`K_Q^delta(w*) = { w : max_{q in Q} d(h_q(w), h_q(w*)) <= delta }`

and let `C_Q^delta(w*)` be the connected component of `K_Q^delta(w*)` containing `w*`.

If `Q subset Q'`, then adding experimental coordinates cannot make the sup-distance constraint easier to satisfy, so

`K_Q'^delta(w*) subset K_Q^delta(w*)`

and therefore

`C_Q'^delta(w*) subset C_Q^delta(w*)`.

This is the clean, assumption-explicit meaning of **experimental refinement**. It is not true for arbitrary ways of renormalizing or changing the distance when experiments are added.

## 2. Robustness should be stated as a tolerance sandwich

Suppose the counterfactual signature used in analysis has uniform error at most `epsilon` per world relative to the true signature, under the same sup metric. Pairwise target-relative distance can then shift by at most `2 epsilon`. This gives the set/component sandwich

`C_Q^(delta-2epsilon) subset C_tilde_Q^delta subset C_Q^(delta+2epsilon)`

when the left tolerance is nonnegative and connectivity is computed in the same underlying world topology/graph.

This is more defensible than claiming that the connected component at one fixed `delta` is itself stable. A component can change abruptly when `delta` crosses a merge threshold.

## 3. Provisional finite-separation / embedding result

Let `X` denote the smooth, compact behavioral quotient of state-law worlds that remain distinct under the full admissible intervention family, with dimension `d`. Suppose each experiment contributes `r` real coordinates and the admissible experiment family is sufficiently rich that collision constraints satisfy the required transversality/genericity conditions.

The theory lane derived the standard embedding-style sufficient regime

`m r > 2 d`

for a generic tuple of `m` experiments to separate behaviorally distinct worlds.

**Interpretation:** this is best viewed as an application of classical transversality/embedding ideas, not a new universal embedding theorem. The key scientific question is whether a biologically realistic perturbation family satisfies the richness assumptions.

### Lower bounds
If `e(X)` is the smallest Euclidean embedding dimension of the behavioral quotient, any continuous exact signature in `R^(mr)` must satisfy

`mr >= e(X)`.

For finite candidate sets, a separate packing argument gives a noise-resolution lower bound. If `N` candidate worlds must remain at least `Delta` apart in an `mr`-dimensional signature box with coordinate range `[-R,R]`, then a volume/packing bound implies a logarithmic lower bound on `mr` as a function of `N`, `R`, and `Delta`. These bounds should be used as sanity checks, not advertised as biological laws.

## 4. Hard negative result: greedy ambiguity splitting can be arbitrarily bad

Define a finite candidate-world graph `G=(V,E)` and a topology-aware utility

`f(Q) = |V| - |C_Q(w*)|`,

where `C_Q(w*)` is the surviving connected component around the truth after experiments `Q`.

The theory lane constructed an `n x n` grid where two individually weak experiments remove complementary gaps in a middle column. Each alone leaves the graph connected, but together they create a full wall and split away roughly half the graph. A third experiment produces a slightly larger one-step reduction, causing greedy selection to choose it first. With budget two, the greedy/optimal utility ratio scales like `O(1/n)` and tends to zero.

Therefore:

> **Connected-component reduction is not generally submodular, and greedy topology-aware experiment selection has no constant-factor guarantee without additional structure.**

### Consequence for this project
For small perturbation libraries, benchmark greedy selection against exhaustive optimal subsets for budgets `k=1,2,3,...`. Search explicitly for complementarity and submodularity violations. Do not justify a greedy policy merely because it works on one simulator.

## 5. State completion is predictive sufficiency under interventions

Let `H` be measured history and `pi` an allowed future intervention policy. Define the future interventional kernel

`K_h^pi = P(future observations | H=h, do(pi))`.

Histories are interventionally predictively equivalent when

`h ~_Pi h'` iff `K_h^pi = K_h'^pi` for every `pi in Pi`.

A measured present state `S=s(H)` is complete for `Pi` when equal `S` values imply this predictive equivalence. Equivalently, under the relevant causal/statistical assumptions, future observations are conditionally independent of older history given `S` and the intervention policy.

The quotient of histories by `~_Pi` is the coarsest fully predictive state. This is strongly precedented by causal states, PSRs and controlled/input-output predictive-state constructions.

**Rejected novelty claim:** “A state that screens off the past from the future is a new mathematical definition of developmental state.”

**Surviving biological hypothesis:** a real, experimentally measurable developmental representation may approximate this minimal interventional predictive quotient for a specified future family.

## 6. Finite-sample calibration is mandatory

A plug-in conditional mutual information statistic `I(Y;H | S,A)` was calibrated on known first-order Markov and deliberately non-Markov simulators. The checkpoint reported:

| N | Markov mean CMI | empirical 95% Markov cutoff | non-Markov mean CMI | power |
|---:|---:|---:|---:|---:|
| 250 | 0.01233 | 0.02975 | 0.01974 | 0.144 |
| 500 | 0.00628 | 0.01510 | 0.01308 | 0.336 |
| 1,000 | 0.00297 | 0.00763 | 0.01187 | 0.748 |
| 2,000 | 0.00152 | 0.00371 | 0.01106 | 0.984 |

These are **simulator calibration results** from the lane and still require code migration/reproduction. They illustrate two important points: the finite-sample null is not zero, and small samples can have very poor power to detect genuine residual history.

No living-system screening-off claim should be made without both known-Markov false-positive calibration and known-non-Markov power calibration at comparable sample sizes.

## 7. Frozen rejections from this pass

- Adding experiments always shrinks ambiguity **without specifying the metric** — rejected.
- Exact connected counterfactual fibers are a novel topological object — rejected; Reeb-space precedent.
- The finite-resolution connected fiber is automatically an approximate bisimulation class — rejected.
- Small measurement noise guarantees small fixed-`delta` component change — rejected.
- Pairwise separability by an arbitrary infinite experiment family always implies a finite separating subset — rejected without compactness/richness assumptions.
- Greedy connected-ambiguity splitting is generally near-optimal — rejected; can be arbitrarily bad.
- Joint inference of hidden state and parameters is a new observability concept — rejected; augmented-state identifiability precedent.
- “Present screens off past” is a novel state principle — rejected; causal-state / predictive-sufficiency precedent.
- A finite vector of action-conditioned future predictions as state is unprecedented — strongly rejected by PSR literature.

## 8. Mandatory next tests

1. **Monotonicity audit:** recompute nested experiment panels under the sup metric and exhaustively verify component inclusion.
2. **Critical-delta persistence:** report merge intervals/thresholds instead of one arbitrary tolerance.
3. **Noise injection:** verify the tolerance sandwich under controlled signature error.
4. **Greedy vs optimal:** enumerate experiment subsets wherever feasible.
5. **Augmented-state observability:** compare local rank-deficient directions with tangent directions of empirical causal fibers.
6. **Embedding test:** estimate behavioral dimension and measure when counterfactual collisions disappear.
7. **PSR audit:** construct a history-by-intervention-test prediction matrix and test whether a small core-test basis spans the remaining predictions.
8. **Completion calibration:** known Markov + non-Markov simulation before any biological claim.

## Current strongest formulation

The program does **not** currently own Reeb fibers, observability, predictive sufficiency, or counterfactual-prediction states. The defensible candidate contribution is a biological/computational synthesis:

> Connected approximate inverse fibers of joint developmental state-law worlds are refined by interventions; state completion asks whether a physically measurable present biological state can approximate the minimal interventionally predictive quotient for a specified developmental task.

That is a falsifiable research program, not a settled new branch of mathematics.
